package org.bk.algo.general;

import java.util.Arrays;

class NumRescueBoats {
    public int numRescueBoats(int[] people, int limit) {
        Arrays.sort(people);
        boolean[] completed = new boolean[people.length];

        for (int i = people.length - 1; i >= 0; i--) {

        }
        return -1;
    }
}